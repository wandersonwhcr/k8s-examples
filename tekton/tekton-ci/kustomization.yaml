namespace: tekton-ci

resources:
  - clusterrolebinding.yaml
  - namespace.yaml
  - pipeline.yaml
  - rolebinding.yaml
  - serviceaccount.yaml
  - task.yaml

commonLabels:
  app.kubernetes.io/name: tekton-ci
