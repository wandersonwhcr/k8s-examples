namespace: prometheus

resources:
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - namespace.yaml
  - service.yaml
  - statefulset.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: config
    files:
      - assets/prometheus.yml
