namespace: drone

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: drone
    envs:
      - .env

resources:
  - deployment.yaml
  - ingress.yaml
  - namespace.yaml
  - role.yaml
  - rolebinding.yaml
  - service.yaml
  - serviceaccount.yaml
  - statefulset.yaml
